{
  "scripts": {
    "smoke": "node scripts/production-smoke.mjs",
    "build": "echo 'static site, no build needed'"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "sharp": "^0.35.1"
  }
}